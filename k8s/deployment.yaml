apiVersion: apps/v1
kind: Deployment
metadata:
  name: platform-suite-v1
  labels:
    app: platform-suite-v1
spec:
  replicas: 3
  selector:
    matchLabels:
      app: platform-suite-v1
  template:
    metadata:
      labels:
        app: platform-suite-v1
    spec:
      containers:
      - name: api
        image: platform-suite-v1:latest
        ports:
        - containerPort: 8000
        env:
        - name: DATABASE_URL
          value: "postgresql://postgres:postgres@db:5432/app"
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 5
