apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: platform-suite-v1-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: platform-suite-v1.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: platform-suite-v1-service
            port:
              number: 80
